# A module I am going to use as an example

def greet(what="Hello There", to="Mystery Person"):
    """
    A convenience function that returns a friendly string
    Parameters:
        what (str): The greeting to issue
        to (str): The person to greet
    Returns:
        a string that greet a person
    """
    return f'{what} {to} ... (changed!)'
          